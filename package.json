{
  "name": "browsermark",
  "version": "1.0.0",
  "description": "Browser-based markdown to PDF converter with diagram support",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@vitest/browser": "^2.0.0",
    "vitest": "^2.0.0",
    "jsdom": "^24.0.0"
  }
}
